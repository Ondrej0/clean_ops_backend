services:
  postgres:
    image: 'postgres:16-alpine'
    environment:
      - 'POSTGRES_DB=cleanops'
      - 'POSTGRES_PASSWORD=cleanops'
      - 'POSTGRES_USER=cleanops'
    ports:
      - '5432:5432'
    volumes:
      - 'postgres_data:/var/lib/postgresql/data'

volumes:
  postgres_data:
